Nueva fase: guardar opcionalmente imagen de planilla en Google Drive.

No modifiques archivos todavía. Solo analiza y propón plan.

Contexto:
Ya funciona:

* guardar PGN real en Google Drive
* carpeta “Chess Games”
* diálogo de metadatos
* appProperties para búsqueda
* biblioteca “Mis partidas”
* abrir partidas desde Drive
* filtros combinables

Nuevo objetivo:
Cuando el usuario guarde una partida en Drive, ofrecer una opción opcional:

“Guardar también imagen de la planilla”

Principios:

* Por defecto debe estar desactivado.
* El usuario debe decidir expresamente si quiere guardar imágenes.
* ChessLens no debe guardar planillas en nube propia.
* Si se guardan imágenes, deben ir al Drive del usuario.
* No tocar motor OCR/PGN.

Casos a soportar:

* una sola planilla
* varias planillas
* imagen 1, imagen 2, etc.

Analiza y propón:

1. Dónde están disponibles actualmente las imágenes de la planilla en frontend:

   * imageUrls
   * imagePaths
   * displayedImageUrl
   * etc.
2. Qué archivos habría que tocar.
3. Si `PgnActions` necesita recibir `imageUrls` como prop.
4. Si habría que tocar `game-detail.tsx` solo para pasar `game.imageUrls` hacia `PgnActions`.
5. Cómo ampliar el diálogo de metadatos para incluir checkbox:

   * ca: “Guardar també la imatge de la planella”
   * es: “Guardar también la imagen de la planilla”
   * en: “Also save the scoresheet image”
6. Cómo subir imágenes a Drive:

   * una imagen: `basefilename_scoresheet-1`
   * varias imágenes: `basefilename_scoresheet-1`, `basefilename_scoresheet-2`, etc.
7. Qué carpeta usar:

   * misma carpeta “Chess Games”
   * subcarpeta “Scoresheets”
   * o subcarpeta por partida
8. Qué opción recomiendas para MVP.
9. Cómo relacionar el PGN con las imágenes:

   * appProperties en el PGN
   * nombres de archivo relacionados
   * Drive file IDs si es posible
10. Riesgos:

* tamaño de imágenes
* CORS/fetch de imágenes
* varias planillas
* privacidad

11. Plan en subfases pequeñas.
12. Checks a ejecutar.

No tocar:

* server/*
* shared/*
* client/src/components/chessboard-viewer.tsx
* client/src/hooks/use-games.ts
* OCR
* PGN engine
* reviewState
* resume
* python-chess
* multi-planilla
* salto de fila
* corrección desde tablero
* idiomas de planilla
* formatos de planilla

No edites nada.
